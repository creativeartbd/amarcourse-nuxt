export default defineEventHandler(async (event) => {
    // const config = useRuntimeConfig();
    // const body = await readBody(event);

    // try {
    //     const response = await $fetch('https://api.amarcourse.com/wp-json/custom/v1/register', {
    //         method: 'POST',
    //         headers: {
    //           'Authorization': 'Basic ' + btoa(`${config.WP_USERNAME}:${config.WP_APPLICATION_PASSWORD}`),
    //           'Content-Type': 'application/json',
    //           'Origin': 'http://localhost:4000', // Manually add Origin header'
    //           'key' : 
    //         },
    //         body: {
    //           username: body.username,
    //           email: body.email,
    //           password: body.password,
    //         },
    //       });
      
    //       return response;
    // } catch (error) {
    //     return error.data;
    // }
    
  });
  